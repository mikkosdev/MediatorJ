package org.example;

/**
 * Every request class must implement this interface.
 *
 * (Currently this interface is a marker interface only with no methods to implement.)
 */
public interface IRequest {
}
